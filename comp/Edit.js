import { SafeAreaView, 
    ScrollView,
     StyleSheet, 
     Text, 
     View,
      Alert,
    Image, 
    RefreshControl, 
ActivityIndicator,
FlatList,
Pressable,
Modal, 
TextInput,
TouchableOpacity} from 'react-native'
import React , {useState, useEffect, useCallback} from 'react'

const Edit = (props) => {
    return (
        <Text>Xin chào Edit</Text>
    )
}

export default Edit

const styles = StyleSheet.create({

})